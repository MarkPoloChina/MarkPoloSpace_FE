<template>
  <div class="mps-comp-index-carousel">
    <el-carousel indicator-position="outside" height="750px" :interval="5000">
      <el-carousel-item
        v-for="item in coverUrl"
        :key="item"
        class="mps-comp-index-carousel-main"
      >
        <el-image :src="item" fit="cover" class="mps-cover-img"></el-image>
      </el-carousel-item>
    </el-carousel>
    <div class="mps-title-container">
      <div class="mps-title">
        <div class="mps-title-blur">
          <div class="mps-title-mp mps-title-text">Mark Polo</div>
          <div class="mps-title-design mps-title-text">
            markpolo.cn Powered by MPSTO
          </div>
          <div class="mps-title-ver mps-title-text">MPS Now Available.</div>
        </div>
      </div>
    </div>
  </div>
</template>
<script setup>
import { getCurrentInstance, onMounted } from "vue";
const coverUrl = getCurrentInstance().proxy.$var.url.covers;
onMounted(() => {});
</script>
<style lang="scss" scoped>
.mps-comp-index-carousel {
  position: relative;

  .mps-comp-index-carousel-main {
    border-radius: 20px;

    .mps-cover-img {
      width: 100%;
      height: 750px;
    }
  }

  .mps-title-container {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    right: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;

    .mps-title {
      display: flex;
      height: 100%;
      width: 100%;
      flex-direction: column;
      justify-content: center;
      align-items: center;

      .mps-title-blur {
        background-color: rgba(0, 0, 0, 0.35);
        backdrop-filter: blur(3px);
        padding: 20px 20px 20px 20px;
        border-radius: 20px;
      }

      .mps-title-text {
        text-align: center;
        color: white;
        font-family: "Raleway";

        &.mps-title-mp {
          margin: 20px 0 20px 0;
          font-size: 100px;
          font-weight: 1000;
        }

        &.mps-title-design {
          font-size: 40px;
          font-weight: 800;
        }

        &.mps-title-ver {
          margin: 10px 0 10px 0;
          font-size: 35px;
          font-weight: 800;
        }
      }
    }
  }
}
</style>
